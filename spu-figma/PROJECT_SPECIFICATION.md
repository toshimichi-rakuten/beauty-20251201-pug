# 楽天ビューティ キャンペーンページ仕様書
**プロジェクト:** SPU Figma Campaign Page
**バージョン:** 1.0.0
**最終更新:** 2025年12月1日

---

## 📋 目次
1. [プロジェクト概要](#プロジェクト概要)
2. [ディレクトリ構造](#ディレクトリ構造)
3. [技術スタック](#技術スタック)
4. [開発環境のセットアップ](#開発環境のセットアップ)
5. [ファイル構成の詳細](#ファイル構成の詳細)
6. [Pugテンプレートシステム](#pugテンプレートシステム)
7. [既存アセットの活用方法](#既存アセットの活用方法)
8. [新規ページ作成手順](#新規ページ作成手順)
9. [ページ編集手順](#ページ編集手順)
10. [ビルド・デプロイ](#ビルドデプロイ)
11. [トラブルシューティング](#トラブルシューティング)

---

## 📌 プロジェクト概要

### 目的
楽天ビューティのSPU（スーパーポイントアッププログラム）キャンペーンページの制作・管理

### 特徴
- **Pugテンプレートエンジン**を使用した効率的なHTML生成
- **共通アセット**（CSS、画像、JSなど）の再利用
- **ライブリロード**機能による開発効率の向上
- **バージョン管理**による適切なキャッシュコントロール

### プロジェクトタイプ
- スタンドアロン型：`index-standalone.pug`（依存関係なし）
- 統合型：`index.pug`（共通レイアウト使用）

---

## 📁 ディレクトリ構造

```
/campaign/
├── spu-figma/                    # 本プロジェクト
│   ├── .git/                     # Gitリポジトリ
│   ├── .gitignore                # Git除外設定
│   ├── README.md                 # プロジェクト説明
│   ├── package.json              # npm設定ファイル
│   ├── package-lock.json         # 依存関係ロック
│   ├── node_modules/             # npmパッケージ
│   │
│   ├── index.pug                 # メインPugファイル（共通レイアウト使用）
│   ├── index.html                # コンパイル済みHTML
│   │
│   ├── index-standalone.pug      # スタンドアロン版Pug
│   ├── index-standalone.html     # スタンドアロン版HTML
│   │
│   └── _template/                # プルされた共通テンプレート
│       ├── index.pug
│       ├── index.html
│       ├── css/
│       │   └── main.css
│       ├── img/
│       └── styl/
│
├── spu/                          # SPU共通アセット（参照先）
│   ├── css/
│   │   ├── main.css             # メインスタイル
│   │   └── splide.min.css       # スライダーライブラリ
│   ├── img/                      # 画像アセット
│   │   ├── rb_logo.png
│   │   ├── rb_coins_left.png
│   │   ├── rb_coins_right.png
│   │   └── ...
│   ├── js/                       # JavaScriptファイル
│   └── _inc_common_header_responsive.pug  # 共通ヘッダー
│
├── _template/                    # 新規ページ作成用テンプレート
│   ├── index.pug                 # ベーステンプレート
│   ├── css/
│   ├── img/
│   └── styl/
│
└── _common/                      # プロジェクト共通アセット
    └── pug/
        └── Base/
            └── _layout.pug       # ベースレイアウト
```

---

## 🛠 技術スタック

### 言語・テンプレート
- **Pug (Jade)**: HTMLテンプレートエンジン
- **HTML5**: 出力形式
- **CSS3**: スタイリング

### ビルドツール
- **Node.js**: JavaScriptランタイム
- **npm**: パッケージマネージャー
- **pug-cli**: Pugコンパイラー
- **browser-sync**: 開発サーバー・ライブリロード
- **concurrently**: 複数コマンド同時実行

### バージョン管理
- **Git**: ソース管理
- **GitHub**: リモートリポジトリ
  - Repository: `toshimichi-rakuten/beauty-20251201-pug`
  - 認証方式: HTTPS + Personal Access Token

---

## 🚀 開発環境のセットアップ

### 必要な環境
- Node.js v14以上
- npm v6以上
- Git

### 初回セットアップ

```bash
# 1. プロジェクトディレクトリに移動
cd /path/to/campaign/spu-figma

# 2. 依存関係のインストール
npm install

# 3. ビルドテスト
npm run build

# 4. 開発サーバー起動
npm run dev
```

### 利用可能なnpmコマンド

| コマンド | 説明 |
|---------|------|
| `npm install` | 依存パッケージをインストール |
| `npm run dev` | 開発サーバー起動（ファイル監視+ライブリロード） |
| `npm run build` | PugをHTMLにコンパイル |
| `npm run watch:pug` | Pugファイルの変更を監視 |
| `npm run serve` | Browser-syncサーバー起動 |

---

## 📄 ファイル構成の詳細

### package.json
```json
{
  "name": "spu-figma",
  "version": "1.0.0",
  "description": "SPU campaign page with Pug",
  "scripts": {
    "dev": "concurrently \"npm run watch:pug\" \"npm run serve\"",
    "watch:pug": "pug -w index-standalone.pug -o . -P",
    "build:pug": "pug index-standalone.pug -o . -P",
    "serve": "browser-sync start --server --files '*.html, ../spu/css/*.css, ../spu/js/*.js' --port 3000",
    "build": "npm run build:pug"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0-alpha6",
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}
```

### .gitignore
```
node_modules/
.DS_Store
*.log
```

---

## 🎨 Pugテンプレートシステム

### 2つのPugファイルの使い分け

#### 1. index.pug（統合型）
**特徴:**
- 共通レイアウト(`_layout.pug`)を継承
- 楽天ビューティの全体的なデザインシステムと統合
- グローバルナビゲーション、フッター自動生成

**使用場面:**
- 本番環境にデプロイする最終版
- 楽天ビューティの他ページと整合性を保つ必要がある場合

**構造:**
```pug
extends ../../../../_common/pug/Base/_layout.pug

block init
  - var meta = { title: "...", description: "..." }
  - var css = "../spu/css/main.css?v20250119"

block addHead
  //- 追加のhead要素

block nav
  +grobalNav('/cnt/topics/')
  +breadcrumb(lists)

block content
  #Spu.spu
    //- メインコンテンツ
```

#### 2. index-standalone.pug（スタンドアロン型）
**特徴:**
- 完全に独立したHTML構造
- 外部依存なし
- 開発・テストが容易

**使用場面:**
- ローカル開発時
- デザインの迅速なプロトタイピング
- 独立したランディングページ

**構造:**
```pug
doctype html
html(lang='ja')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title SPU
    link(rel='stylesheet', href='../spu/css/main.css')

  body
    #Spu.spu
      //- メインコンテンツ
```

---

## 🔧 既存アセットの活用方法

### 1. CSS（スタイルシート）

#### メインスタイル
```pug
//- 相対パスで参照
link(rel='stylesheet', href='../spu/css/main.css?v20250119')
```

**場所:** `../spu/css/main.css`
**内容:**
- SPUキャンペーン共通スタイル
- レスポンシブデザイン設定
- アニメーション定義

**バージョンパラメータ:**
- `?v20250119` 形式でキャッシュコントロール
- 更新時は日付を変更

#### ライブラリCSS
```pug
//- Splideスライダー
link(rel='stylesheet', href='../spu/css/splide.min.css')
```

### 2. 画像アセット

#### 画像の配置場所
```
../spu/img/
├── rb_logo.png              # 楽天ビューティロゴ
├── rb_coins_left.png        # コイン画像（左）
├── rb_coins_right.png       # コイン画像（右）
├── rb_coins_panda.png       # パンダコイン
├── announce-title-icon.png  # アナウンスアイコン
└── ogp_spu.png              # OGP画像
```

#### 使用例
```pug
//- 基本的な画像
img(src="../spu/img/rb_logo.png", alt="楽天ビューティ", width='100%', height='auto')

//- レスポンシブ表示制御
img.rb_logo.-pc(src="../spu/img/rb_logo.png")
img.rb_logo.-sp(src="../spu/img/rb_logo_sp.png")
```

### 3. 共通コンポーネント

#### ヘッダーインクルード
```pug
//- SPU共通ヘッダーを読み込み
include ../spu/_inc_common_header_responsive
```

**場所:** `../spu/_inc_common_header_responsive.pug`
**内容:** SPUキャンペーン共通のヘッダーデザイン

#### キャンペーンタグ
```pug
#CpnTags
  -
    var taglist = {
      tag: [
        'エントリー不要',
        'ネット予約・施術完了',
        '常時開催',
      ],
    }
  +campaignTag(taglist)
```

### 4. JavaScript

#### 外部スクリプト読み込み
```pug
//- PITARI（エリアターゲティング）
script(src='//www.rakuten.co.jp/com/advance/beauty/AreaTarget_SPU_2021/responsive/condition.js')

//- カスタムスクリプト
script(src='../spu/js/main.js')
```

---

## 📝 新規ページ作成手順

### ステップ1: テンプレートの複製

```bash
# campaignディレクトリで実行
cd /path/to/campaign

# テンプレートをコピー
cp -r _template new-campaign-name

# 新しいディレクトリに移動
cd new-campaign-name
```

### ステップ2: npmプロジェクトのセットアップ

```bash
# package.jsonを作成
cat > package.json <<'EOF'
{
  "name": "new-campaign-name",
  "version": "1.0.0",
  "description": "New campaign page with Pug",
  "scripts": {
    "dev": "concurrently \"npm run watch:pug\" \"npm run serve\"",
    "watch:pug": "pug -w index-standalone.pug -o . -P",
    "build:pug": "pug index-standalone.pug -o . -P",
    "serve": "browser-sync start --server --files '*.html, ../spu/css/*.css' --port 3000",
    "build": "npm run build:pug"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0-alpha6",
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}
EOF

# 依存関係をインストール
npm install
```

### ステップ3: index.pugの編集

```pug
extends ../../../../_common/pug/Base/_layout.pug

block init
  -
    var parameter = "?v20251201"  // 更新
    var meta = {
      title: "新キャンペーンタイトル",  // 更新
      directory: "new-campaign-name",  // 更新
      description: "キャンペーンの説明文",  // 更新
      url: "https://beauty.rakuten.co.jp/cnt/topics/campaign/new-campaign-name/",  // 更新
      og_image: "https://beauty.rakuten.co.jp/cnt/topics/campaign/new-campaign-name/img/ogp.png" + parameter,
    }
    var css = "css/main.css" + parameter

block content
  #NewCampaign.new-campaign
    // ここにコンテンツを記述
```

### ステップ4: スタンドアロン版の作成

```pug
doctype html
html(lang='ja')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title 新キャンペーンタイトル
    meta(name='description', content='キャンペーンの説明文')

    //- 共通CSSを参照
    link(rel='stylesheet', href='../spu/css/main.css?v20251201')
    //- カスタムCSS
    link(rel='stylesheet', href='css/main.css?v20251201')

  body
    #NewCampaign.new-campaign
      //- メインビジュアル
      section.hero
        h1 キャンペーンタイトル
        img(src='img/hero.png', alt='メインビジュアル')

      //- コンテンツセクション
      section.content
        h2 キャンペーン詳細
        p キャンペーンの詳細説明
```

### ステップ5: CSS/画像の準備

```bash
# CSSディレクトリ作成
mkdir -p css

# 画像ディレクトリ作成
mkdir -p img

# 基本CSSファイル作成
cat > css/main.css <<'EOF'
/* カスタムスタイル */
.new-campaign {
  /* ページ固有のスタイル */
}

.hero {
  /* ヒーローセクション */
}
EOF
```

### ステップ6: ビルドとテスト

```bash
# ビルド
npm run build

# 開発サーバー起動
npm run dev

# ブラウザで確認
# http://localhost:3000
```

### ステップ7: Git管理

```bash
# Gitリポジトリ初期化
git init

# .gitignore作成
cat > .gitignore <<'EOF'
node_modules/
.DS_Store
*.log
EOF

# 初回コミット
git add .
git commit -m "Initial commit: New campaign page setup"

# リモートリポジトリに接続
git remote add origin https://[PAT]@github.com/ユーザー名/リポジトリ名.git
git push -u origin main
```

---

## ✏️ ページ編集手順

### 日常的な編集フロー

#### 1. 開発サーバーの起動
```bash
cd /path/to/campaign/your-campaign
npm run dev
```

ブラウザが自動で開き、`http://localhost:3000`でページを確認できます。

#### 2. Pugファイルの編集

**index-standalone.pug** を編集（推奨）

```pug
//- テキスト変更例
p.description
  | キャンペーン期間：2025年12月1日〜12月31日

//- 画像変更例
img(src='../spu/img/new-banner.png', alt='新しいバナー')

//- リンク追加例
a.btn(href='https://beauty.rakuten.co.jp/campaign/')
  | 詳細はこちら
```

#### 3. 自動リロードで確認

ファイルを保存すると：
1. Pugが自動的にHTMLにコンパイル
2. ブラウザが自動リロード
3. 変更が即座に反映される

#### 4. CSSの編集

```bash
# カスタムCSSファイルを編集
vi css/main.css
```

```css
/* 新しいスタイル追加 */
.new-section {
  padding: 40px 20px;
  background-color: #f5f5f5;
}

.new-section h2 {
  font-size: 24px;
  color: #bf0000;
}
```

保存すると自動的にブラウザがリロードされます。

### よく使うPug構文

#### 変数の定義と使用
```pug
-
  var campaignTitle = "ポイント10倍キャンペーン"
  var startDate = "2025年12月1日"
  var endDate = "2025年12月31日"

h1= campaignTitle
p 期間：#{startDate}〜#{endDate}
```

#### 条件分岐
```pug
- var isActive = true

if isActive
  p.active-message キャンペーン実施中！
else
  p.inactive-message キャンペーンは終了しました
```

#### 繰り返し
```pug
-
  var benefits = [
    'ポイント10倍',
    '送料無料',
    '限定クーポン配布'
  ]

ul.benefits-list
  each benefit in benefits
    li.benefit-item= benefit
```

#### コメント
```pug
//- このコメントはHTMLに出力されない

// このコメントはHTMLに出力される
```

---

## 🏗 ビルド・デプロイ

### ローカルビルド

```bash
# HTMLを生成
npm run build

# 生成されたファイル確認
ls -la *.html
```

### デプロイ前チェックリスト

- [ ] バージョンパラメータの更新（`?v20251201`）
- [ ] 画像パスの確認
- [ ] リンクの動作確認
- [ ] レスポンシブデザインの確認
- [ ] ブラウザ互換性テスト
- [ ] OGP画像の設定確認

### Git コミット・プッシュ

```bash
# 変更を確認
git status

# ステージング
git add .

# コミット
git commit -m "Update: キャンペーン内容の更新

- メインビジュアル変更
- 期間延長対応
- 特典内容追加"

# プッシュ
git push origin main
```

### 本番環境デプロイ

```bash
# index.pugから最終HTMLを生成
pug index.pug -o . -P

# 生成されたindex.htmlを確認
open index.html

# サーバーにアップロード
# （楽天ビューティの標準デプロイ手順に従う）
```

---

## 🔍 トラブルシューティング

### npm install が失敗する

**症状:**
```
npm ERR! code EACCES
```

**解決方法:**
```bash
# node_modulesを削除
rm -rf node_modules package-lock.json

# 再インストール
npm install
```

### Pugのコンパイルエラー

**症状:**
```
Error: unexpected token "indent"
```

**原因:** インデントの不一致

**解決方法:**
- Pugはインデントが重要（スペース2個 or タブ）
- エディタの設定を確認
- 混在しないように統一

### CSSが反映されない

**チェック項目:**
1. ファイルパスが正しいか
2. バージョンパラメータが更新されているか
3. ブラウザのキャッシュをクリア

```bash
# 強制リロード
# Mac: Cmd + Shift + R
# Windows: Ctrl + Shift + R
```

### 画像が表示されない

**確認項目:**
1. 画像ファイルの存在確認
   ```bash
   ls -la ../spu/img/rb_logo.png
   ```

2. パスの確認
   ```pug
   //- 正しい相対パス
   img(src='../spu/img/rb_logo.png')

   //- 間違い
   img(src='spu/img/rb_logo.png')
   ```

### 開発サーバーが起動しない

**症状:**
```
Error: Port 3000 is already in use
```

**解決方法:**
```bash
# ポートを使用中のプロセスを確認
lsof -i :3000

# プロセスを終了
kill -9 [PID]

# または別のポートを使用
browser-sync start --server --port 3001
```

### Gitプッシュが失敗する

**参考:** Git Helperスキルの詳細なトラブルシューティング参照

**クイック解決:**
```bash
# 他の成功しているリポジトリの設定を確認
cd 他の成功しているリポジトリ
git remote -v

# PATを確認して使用
git remote set-url origin https://[PAT]@github.com/ユーザー名/リポジトリ名.git
git push origin main
```

---

## 📚 参考資料

### 内部ドキュメント
- [テンプレートの種類について](https://confluence.rakuten-it.com/confluence/pages/viewpage.action?pageId=2443993509)
- [Campaign Tagルール](https://confluence.rakuten-it.com/confluence/pages/viewpage.action?pageId=2688949406)

### 外部ドキュメント
- [Pug公式ドキュメント](https://pugjs.org/)
- [Browser-sync公式ドキュメント](https://browsersync.io/)
- [npm Scripts](https://docs.npmjs.com/cli/v8/using-npm/scripts)

### GitHubリポジトリ
- **URL:** https://github.com/toshimichi-rakuten/beauty-20251201-pug
- **認証方式:** HTTPS + Personal Access Token

---

## 📞 サポート

### 質問・問題報告
- **内部:** Confluence / Slack
- **GitHub:** Issue作成

### 更新履歴
| 日付 | バージョン | 内容 |
|------|----------|------|
| 2025-12-01 | 1.0.0 | 初版作成 |

---

**Document Owner:** OMID Development Team
**Last Updated:** 2025-12-01
**Status:** Active
